[package]
name = "sindon_core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Core types for the sindon UI framework"
license.workspace = true
repository.workspace = true
readme.workspace = true
keywords = ["gui", "ui", "geometry", "theme", "sindon"]
categories = ["gui", "data-structures"]

# No dependencies on purpose: this crate is the shared vocabulary every other
# sindon crate builds on, so nothing it names can drag a third-party type into
# their public APIs.
[dependencies]
