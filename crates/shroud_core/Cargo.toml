[package]
name = "shroud_core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Core types for the shroud UI framework"

[dependencies]
slotmap = { workspace = true }
